[project]
name = "agent-fault-lab-langgraph"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["agent-fault-lab", "langgraph==1.2.11"]

[tool.uv]
package = false

[tool.uv.sources]
agent-fault-lab = { path = "../..", editable = true }
